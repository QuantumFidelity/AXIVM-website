import React from 'react';

export const AccordionSection = ({ children }: { children: React.ReactNode }) => (
  <div>{children}</div>
);

export const Accordion = ({ children }: { children: React.ReactNode }) => (
  <div>{children}</div>
);
